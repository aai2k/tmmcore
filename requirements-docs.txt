# MkDocs 2.0 removes the plugin system and the theming system with no migration
# path, which breaks Material entirely. Pinned below it deliberately; revisit
# only when Material ships a release supporting 2.x.
mkdocs>=1.6,<2
mkdocs-material>=9.5,<10
